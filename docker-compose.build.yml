# Use this to build images locally instead of pulling from ghcr.io:
#   docker compose -f docker-compose.yml -f docker-compose.build.yml up --build
services:
  bricoprohq:
    build:
      context: .
    image: ghcr.io/titatom/bricoprohq:local
