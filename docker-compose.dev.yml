# Development/local-build overrides — use with:
#   docker compose -f docker-compose.yml -f docker-compose.dev.yml up --build
services:
  bricoprohq:
    build:
      context: .
    image: ghcr.io/titatom/bricoprohq:local
